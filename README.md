# Travel Map (Flutter) #

```sh
flutter clean
flutter pub get

# launch the map in chrome
flutter run -d chrome
```

Build for web
```sh
flutter clean
flutter build web
flutter run -d chrome
```

Images and galleries moved to [`web/globe/`](web/globe/).